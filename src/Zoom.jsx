import cert from "images/certify.jpg"


const Zoom = ()=>{

    return(
        <Main className="flex flex-row">
            <section>
                <img src={cert} alt="certificate" />
            </section>
            <section>
            </section>

        </Main>
    )
}